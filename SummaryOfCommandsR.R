###########################################
# Summary of R Commands
# Lars Mehwald
###########################################

# open information page for a specific command
help(CommandName)
?CommandName # both function the same way

# find datasets in R
data()

# load dataset from inside of R
data(NameOfDataset)

# delete dataset
rm(NameOfDataset)

############################################

# creating a histogram from a variable
hist(VarName)

# get the mean of a variable
mean(VarName)

